#include <stdio.h>
#include <string.h>
/**
 *_strcat - concatenates two strings.
 *@dest: char*
 *@src: char*
 *Return: char
 */

char *_strcat(char *dest, char *src)
{
char str =  strcat(dest, src);
return (str);
}
