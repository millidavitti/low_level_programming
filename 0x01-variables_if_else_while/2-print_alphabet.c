#include <stdio.h>

/**
 *main - prints the alphabet in lowercase, followed by a new line.
 *
 *Return: void
 */

int main()
{
int ascii = 97;

while(ascii < 123)
{
putchar(ascii);
ascii++;
}
putchar(\n)
return (0);
}
