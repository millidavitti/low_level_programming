#include <stdio.h>

/**
 *print_chessboard - prints the chessboard.
 *@a: board
 */

void print_chessboard(char (*a)[8])
{
int x, y;
for (x = 0; a[x]; x++)
{
for (y = 0; a[y]; y++)
{
putchar(a[x][y]);
}
putchar('\n');
}
}
